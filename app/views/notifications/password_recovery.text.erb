Please copy and paste the following link in your browser to recover your user:

<%= recovery_session_url(:token => @token, :host => '0.0.0.0', :port => 3000) %>

You can use it until <%= @until_time.localtime.to_s(:long) %>.

------

The Forums Staff